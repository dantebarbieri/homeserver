# Custom error pages for proxy hosts.
# Auto-included by NPM in every proxy host server block.

error_page 502 503 504 /502.html;

location = /502.html {
    root /data/nginx/default_www;
    internal;
}
